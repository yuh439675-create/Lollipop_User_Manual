{
  "scripts": {
    "build": "gitbook build && node scripts/fix-favicon.js",
    "serve": "gitbook serve"
  },
  "dependencies": {
    "gitbook-plugin-expandable-chapters": "^0.2.0",
    "graceful-fs": "^4.2.11"
  }
}
